/** @type { import('@storybook/server').Preview } */
// Import fonts.
import "./_fonts.scss";

const preview = {
  parameters: {
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },
  },
};

export default preview;
