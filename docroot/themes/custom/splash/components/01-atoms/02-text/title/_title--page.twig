{{ title_prefix }}
{% if title %}
  {{ include('splash:heading', {
    heading_level: 'h1',
    heading: title,
    heading_element: 'title',
    heading_block: 'page'
  }) }}
{% endif %}
{{ title_suffix }}
