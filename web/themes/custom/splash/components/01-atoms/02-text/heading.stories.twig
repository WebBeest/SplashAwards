{% stories headings with {
  title: 'Splash Awards/Atoms/Heading'
} %}
  {% story heading1 with {
    name: 'Heading 1',
    args: {
      heading_level: 'h1',
      heading: 'This is a page title, level 1 heading',
      bem_heading_block: 'page',
      bem_heading_element: 'title',
    }
  } %}
    {{ include('splash:heading', {heading_level, heading}) }}
  {% endstory %}

  {% story heading2 with {
    name: 'Heading 2',
    args: {
      heading_level: 'h2',
      heading: 'This is a subtitle, level 2 heading'
    }
  } %}
    {{ include('splash:heading', {heading_level, heading}) }}
  {% endstory %}

  {% story heading2_link with {
    name: 'Heading 2 - Link',
    args: {
      heading_level: 'h2',
      heading: 'This is a subtitle with link, level 2 heading',
      heading_url: 'https://www.example.com'
    }
  } %}
    {{ include('splash:heading', {heading_level, heading, heading_url}) }}
  {% endstory %}

  {% story heading3 with {
    name: 'Heading 3',
    args: {
      heading_level: 'h3',
      heading: 'This is a level 3 heading'
    }
  } %}
    {{ include('splash:heading', {heading_level, heading}) }}
  {% endstory %}

  {% story heading4 with {
    name: 'Heading 4',
    args: {
      heading_level: 'h4',
      heading: 'This is a level 4 heading'
    }
  } %}
    {{ include('splash:heading', {heading_level, heading}) }}
  {% endstory %}

  {% story heading5 with {
    name: 'Heading 5',
    args: {
      heading_level: 'h5',
      heading: 'This is a level 5 heading'
    }
  } %}
    {{ include('splash:heading', {heading_level, heading}) }}
  {% endstory %}
{% endstories %}
